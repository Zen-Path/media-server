{
    "devDependencies": {
        "prettier-plugin-sort-json": "^4.2.0",
        "vite": "^7.3.1"
    },
    "scripts": {
        "gen": "python scripts/gen_constants.py",
        "build": "npm run gen && vite build",
        "watch": "npm run gen && vite build --watch"
    }
}
